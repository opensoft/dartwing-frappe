# Dartwing Frappe Development Environment
# This container connects to the existing Frappe infrastructure

# User configuration
# IMPORTANT: Set your username here. This will be the user inside the container.
# Falls back to 'vscode' if not set (generic default for portability)
# Example: USER=brett
# USER=
# UID=1000
# GID=1000

# Database configuration (uses existing frappe-mariadb container)
DB_HOST=frappe-mariadb
DB_PORT=3306
DB_PASSWORD=frappe

# Redis configuration (uses existing frappe redis containers)
REDIS_CACHE=frappe-redis-cache:6379
REDIS_QUEUE=frappe-redis-queue:6379
REDIS_SOCKETIO=frappe-redis-socketio:6379

# Shared bench configuration
# Path to the existing frappe-bench directory on the host machine
# Example: FRAPPE_BENCH_PATH=/home/your-username/projects/frappe/development/frappe-bench
# FRAPPE_BENCH_PATH=
# Path to the Dartwing Frappe app within the bench (mounted into the dev container)
# Example: FRAPPE_DARTWING_APP_PATH=/home/your-username/projects/frappe/development/frappe-bench/apps/dartwing_frappe
# FRAPPE_DARTWING_APP_PATH=

# Frappe site configuration
SITE_NAME=dartwing.localhost
ADMIN_PASSWORD=admin
